/**
 * Commit type enum
 * @enum
 */
export enum CommitEnum {
    shortMint = 0,
    shortBurn = 1,
    longMint = 2,
    longBurn = 3,
}

// side types
export enum SideEnum {
    long = 0,
    short = 1,
}